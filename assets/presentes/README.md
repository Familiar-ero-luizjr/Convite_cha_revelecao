# Imagens dos presentes

As imagens enviadas pelo painel administrativo são commitadas automaticamente nesta pasta pelo Cloudflare Worker.

Os antigos arquivos `fralda.png`, `mamadeira.png` e `sapatinho.png` eram placeholders vazios (0 bytes) e foram removidos.
